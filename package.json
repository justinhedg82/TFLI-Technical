{
  "name": "tfli-technical",
  "version": "1.0.0",
  "description": "Playwright end-to-end test suite for the TFLI technical interview",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/node": "^26.1.2"
  }
}
